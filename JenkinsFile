pipeline {
  agent any
  stages {
    stage('Checkout') {
      steps { checkout scm }
    }
    stage('Verify') {
      steps {
        sh 'echo Hello from Jenkins'
        sh 'git --version'
        sh 'ls -la'
      }
    }
  }
}
